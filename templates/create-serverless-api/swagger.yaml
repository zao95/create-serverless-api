swagger: '2.0'

info:
    version: '1.0'
    title: 'projectCake'
    x-cdk-region: ap-northeast-2
    x-cdk-s3-bucket-name: project-cake-layers2222222

# basePath: /v2

paths:
    /cake:
        get:
            x-cdk-lambda-handler: ./api/getCake.handler # handler path
            # x-cdk-additional-library: [md5]
            description: i get existing cake. cake party!
            produces:
                - application/json
            # parameters:
            # - in: query
            #   name: test
            #   required: true
            #   type: integer
            #   description: The test
            responses:
                200:
                    description: Returns the cake!
                    schema:
                        type: object
